# GitHub-Profile-Analyzer-API
GitHub Profile Analyzer API is a backend application built using Node.js, Express.js, MySQL, and the GitHub Public API. The application allows users to analyze GitHub profiles by providing a GitHub username, automatically fetching public profile information from GitHub, extracting useful insights, and storing the analyzed data in a MySQL database.
